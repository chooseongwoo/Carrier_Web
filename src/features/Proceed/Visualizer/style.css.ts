import { style } from '@vanilla-extract/css';

export const AudioState = style({
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'center',
  alignItems: 'center',
  marginTop: '10px',
  width: '100%',
});
