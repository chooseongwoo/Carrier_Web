import { style } from '@vanilla-extract/css';

export const container = style({
  paddingLeft: '80px',
  width: 'calc(100% - 360px)',
  height: '100vh',
  display: 'flex',
  flexDirection: 'row',
});
