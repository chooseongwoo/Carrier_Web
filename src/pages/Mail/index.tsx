import Sidebar from 'pages/Mail/ui/Sidebar';
import * as s from './style.css';

const Mail = () => {
  return (
    <div className={s.container}>
      <Sidebar />
    </div>
  );
};

export default Mail;
